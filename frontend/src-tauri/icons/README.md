# Native desktop icons

`icon.png` is a checked-in native build asset consumed by Tauri's generated application
context. It is not a browser-only decoration and must remain available to native builds.

The current icon is a deliberately simple project placeholder while installer/brand assets
remain pre-production. Packaging work can replace it with the final cross-platform icon set
without changing the application contract.
